        with:
          node-version: 18
          cache: 'npm'   # built-in caching for node_modules

      - name: Install dependencies
        run: npm ci      # clean install respects lock file

      - name: Run tests (placeholder)
        run: npm test || echo "no test script yet"


